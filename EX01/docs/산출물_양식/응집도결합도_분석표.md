# 응집도 / 결합도 분석표 (B-Member)

> 작성자: ________   작성일: ________

## 1. 구성요소별 판정

각 구성요소의 응집도(7단계 중)와 결합도(6단계 중)를 판정하고 근거를 적는다.

| 구성요소 | 응집도 | 결합도 | 판정 근거(한 줄) |
|:--|:--|:--|:--|
| MemberController |  |  |  |
| MemberDAO |  |  |  |
| MemberDTO |  |  |  |
| DataSourceConfig |  |  |  |
| GlobalExceptionHandler |  |  |  |

> 참고: 응집도(높을수록 좋음) 기능적 > 순차적 > 교환적 > 절차적 > 시간적 > 논리적 > 우연적
> 결합도(낮을수록 좋음) 자료 < 스탬프 < 제어 < 외부 < 공통 < 내용

## 2. 계층 분리 전 / 후 비교

한 컨트롤러 메서드에 요청 수신·검증·DB 접근을 다 넣었을 때와,
Controller / DAO / DTO 로 나눴을 때를 비교한다.

| 관점 | 분리 전 (한 메서드에 다 넣음) | 분리 후 (Controller → DAO + DTO) |
|:--|:--|:--|
| 응집도 |  |  |
| 결합도 |  |  |
| 변경 영향 범위 |  |  |
| 재사용성 |  |  |

## 3. 결론 (3~4줄)

```
(MVC 계층 분리가 응집도·결합도에 준 효과를 요약한다.)
```
